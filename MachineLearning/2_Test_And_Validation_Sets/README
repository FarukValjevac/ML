Code overview:
1_Test_And_Validation_Set.py
    After doing this exercise, you'll know how to do the following:
      * Split a training set into a smaller training set and a validation_set.
      * Analyze deltas between training set and validation set results.
      * Test the trained model with a test set to determine whether your trained model is overfitting.
      * Detect and fix a common training problem.